node {

    stage('checkout') {
        checkout scmGit(branches: [[name: 'master']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/Constelaciones99/cachina.git']])
    }
   
    stage('checkout') {
        sh 'echo "hola, desde hook git"'
        sh 'ls -la'
    }
   
}
